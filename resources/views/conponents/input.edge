<div class="form-group">
    <label for="{{ name }}">
        {{ label || name }}
    </label>
    <input type="{{ type || 'text' }}" name="{{ name }}" id="{{ name }}" class="{{ cls || 'form-control' }}">
</div>